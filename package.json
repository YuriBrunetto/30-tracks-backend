{
  "name": "30-tracks-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "deploy": "git push && git push heroku master"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.3",
    "querystring": "^0.2.0",
    "request": "^2.81.0"
  }
}
